[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/streamvault run build"]

[start]
cmd = "npx serve artifacts/streamvault/dist/public -l $PORT --single"
